#ifndef MS_UI_HPP
#define MS_UI_HPP

#include <ms/Grid.hpp>

namespace ui {

/**
 * @brief show the grid of cells
 * @param grid the Grid object
 */
void show_grid(const ms::Grid& grid);

}  // namespace ui

#endif
